---
- name: Configure firewall policy
  hosts: fortigate
  collections:
    - fortinet.fortios
  connection: httpapi
  gather_facts: no
  vars:
    vdom: "root"
    ansible_httpapi_use_ssl: yes
    ansible_httpapi_validate_certs: no
    ansible_httpapi_port: 7443
    access_token: "jt6QNmh0Ngny1mmq3yngkbkyHmzbNs"  # Replace with your actual token
    ansible_network_os: fortinet.fortios.fortios  # Ensure correct syntax with ":" instead of "="

  tasks:
    - name: Create Firewall Policy
      fortios_firewall_policy:
        vdom: "{{ vdom }}"
        access_token: "{{ access_token }}"
        state: "present"
        firewall_policy:
          policyid: 53  # Ensure policyid is unique and correctly specified
          name: "Ansible Created 1 Lab-Center to WAN Policy"
          srcintf: [{"name": "Lab-Center"}]
          dstintf: [{"name": "virtual-wan-link"}]
          srcaddr: [{"name": "Windows yogi drl servers"}]
          dstaddr: [{"name": "all"}]
          action: "deny"
          schedule: "always"
          service: [{"name": "ALL"}]
          logtraffic: "all"
      register: result

    - name: Debug API Response
      debug:
        var: result
